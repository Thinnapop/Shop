npm init => to initialize the package.json file
npm init - y => to initialize the package.json file แบบรีบ no quesiton asked

npm install nodemon => 
or use npm uninstall nodemon for uninstall the module from json
npm install nodemon -D => for install the devDependencies ,automatic reset if code has modified
Nodemon will point to the main in package.json then we can use it for run whatever file using "npm run dev"

npm install express => develop the nodeJS 
npm install express -g => -g mean global , so we can call it where ever in our device 


//Package.json is like our personal ID for identify our program